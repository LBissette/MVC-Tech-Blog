{{> new-post}}

{{#each postsAndComments}}
    {{#if this.post_id}}
        {{> comment}}
    {{/if}}

    {{#unless this.post_id}}
        {{> blogpost}}
    {{/unless}}
{{/each}}

<script src="/link.js"></script>
